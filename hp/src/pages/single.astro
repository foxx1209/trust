---
// Config

// Layout
import Layout from "@layouts//Layout.astro";
import {Image} from "astro:assets";
import dummy from "../assets/images/dummy.png";
import Button from "../components/Button.astro";
import Close from "@components/Close.astro";
---

<Layout title="DX推進の成功戦略 | Nexus Strategy"  description="デジタルトランスフォーメーションを成功に導くための実践的アプローチ">
  <div class="single" data-aos="fade-up" data-aos-duration="3000">
    <div class="single-header">
      <div class="single-category-wrapper">
        <div class="single-category">
          <p class="single-category__text">カテゴリが入ります。</p>
        </div>
        <h2 class="single__title">ここにタイトルが入ります。</h2>
      </div>
    </div>
    <article class="post">
      <div class="post__content">
      <div class="post__inner">
      <figure class="post__img">
        <Image src={dummy} alt="デジタルトランスフォーメーション戦略" />
      </figure>
      <div class="post__textarea">
        <p class="post__text">デジタルトランスフォーメーション（DX）は、単なるIT化ではありません。ビジネスモデルそのものを変革し、新たな価値を創造するための戦略的取り組みです。<br><br>多くの企業がDXの必要性を認識しながらも、具体的な進め方に悩んでいます。成功の鍵は、明確なビジョンの設定、段階的な実行計画、そして組織全体の変革マインドセットにあります。<br><br>本記事では、DX推進における重要なポイントと、実際の成功事例から学ぶべき教訓について解説します。データ基盤の構築からビジネスモデルの変革まで、包括的なアプローチが求められます。<br><br>また、DXは経営層のコミットメントが不可欠です。トップダウンとボトムアップの両方のアプローチを組み合わせることで、組織全体を巻き込んだ変革が実現します。</p>
      </div>
    </div>
  </div>
  </div>
  <div class="post__link">
    <Button label="コラム一覧に戻る" link="/column" reverse />
  </div>
    </article>
  </div>
    <Close />

</Layout>
<style>
  .single {
margin-block-start: calc(100* var(--torem));
padding-bottom: calc(100* var(--torem));
@media(width >= 48rem) {
  margin-block-start: calc(235* var(--torem));
  padding-bottom: calc(150* var(--torem));
}
}

.single-header {
background: linear-gradient(135deg, #000000 0%, #2a2a2a 100%);
box-shadow: 2px 4px 15px rgba(0, 0, 0, 0.05);
border-radius: 20px 20px 0px 0px;
position: relative;
z-index: 1;
padding: calc(20* var(--torem)) calc(20* var(--torem)) calc(150* var(--torem));
@media(width >= 48rem) {
  padding: calc(30* var(--torem)) calc(120* var(--torem)) calc(295* var(--torem));
}

}
.single-header::before{
  content: "";
    background: url("../assets/images/bg.webp") no-repeat center / cover;
    border-radius: 20px;
    position: absolute;
    inset: 0;
    z-index: -1;
}
.single-category-wrapper{
  max-inline-size: calc(1200* var(--torem));
  inline-size: 100%;
  margin-inline: auto;

}
.single-category {
background-color: var(--white);
inline-size: fit-content;
padding: calc(3* var(--torem)) calc(5* var(--torem));
}

.single-category__text {
font-size: calc(14* var(--torem));
letter-spacing: 0.1em;
font-weight: 700;
background: linear-gradient(135deg, #000000 0%, #2a2a2a 100%);
-webkit-background-clip: text;
-webkit-text-fill-color: transparent;
background-clip: text;
text-fill-color: transparent;
}

.single__title {
font-size:calc(30* var(--torem)) ;
color: var(--white);
font-weight: 700;
margin-block-start: calc(13* var(--torem));
@media(width >= 48rem) {
  font-size:calc(42* var(--torem)) ;

}
}
.post{
  margin-block-start: calc(-100* var(--torem));
  z-index: 1;
  position: relative;
  @media(width >= 48rem) {
    margin-block-start: calc(-256* var(--torem));
  }
}

.post__content {
max-inline-size: calc(1320* var(--torem));
inline-size: 100%;
margin-inline: auto;
background: var(--off-white);
border-radius: 20px;
padding: calc(40* var(--torem)) 0;
@media(width >= 48rem) {
  padding: calc(80* var(--torem)) 0;
}

}

.post__inner {
max-inline-size: calc(970* var(--torem));
inline-size: 100%;
margin-inline: auto;
padding: 0 calc(20* var(--torem));

}

.post__img {
max-inline-size: calc(930* var(--torem));
inline-size: 100%;
margin-inline: auto;
img{
  aspect-ratio:930 / 380 ;
}
}

.post__textarea {
margin-block-start: calc(20* var(--torem));
@media(width >= 48rem) {
  margin-block-start: calc(30* var(--torem));
}
}

.post__text {
  font-size: calc(14* var(--torem));
  font-weight: 500;
  letter-spacing: 0.1em;
  line-height: 2.5;
  @media(width >= 48rem) {
    font-size: calc(16* var(--torem));
  }

}

.post__link {
margin-inline: auto;
display: grid;
place-items: center;
margin-block-start: calc(40* var(--torem));
}


</style>
